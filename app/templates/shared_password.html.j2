<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mot de passe partagé</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">

<div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full">
    <h2 class="text-2xl font-semibold text-center mb-6">Mot de passe partagé</h2>

    <div class="space-y-4">
        <div>
            <label class="block text-gray-700 font-medium">Titre</label>
            <div class="border border-gray-300 rounded-md px-4 py-2 bg-gray-50">{{ shared_entry.title }}</div>
        </div>

        <div>
            <label class="block text-gray-700 font-medium">Nom d'utilisateur</label>
            <div class="border border-gray-300 rounded-md px-4 py-2 bg-gray-50">{{ shared_entry.username }}</div>
        </div>

        <div>
            <label class="block text-gray-700 font-medium">Email</label>
            <div class="border border-gray-300 rounded-md px-4 py-2 bg-gray-50">{{ shared_entry.email }}</div>
        </div>

        <div>
            <label class="block text-gray-700 font-medium">Mot de passe</label>
            <div class="border border-gray-300 rounded-md px-4 py-2 bg-gray-50 font-mono break-all">{{ shared_entry.password }}</div>
        </div>

        {% if shared_entry.url %}
        <div>
            <label class="block text-gray-700 font-medium">URL</label>
            <a href="{{ shared_entry.url }}" target="_blank" class="text-indigo-600 hover:underline break-all">{{ shared_entry.url }}</a>
        </div>
        {% endif %}

        <div>
            <label class="block text-gray-700 font-medium">Expiration</label>
            <div class="text-sm text-gray-500">{{ shared_entry.expiry_date.strftime('%d/%m/%Y à %H:%M') }}</div>
        </div>
    </div>

    <div class="mt-6 text-center">
        <a href="/" class="text-indigo-600 hover:underline">Retour à l'accueil</a>
    </div>
</div>

</body>
</html>
